name: json
on: push
jobs: 
    jobe1:
        runs-on: ubuntu-latest
        steps:
        - name: checkout
          uses: actions/checkout@v4
        - name: step1
          run: |
           echo " my first runner"
           ls -ltra
           cat README.md


